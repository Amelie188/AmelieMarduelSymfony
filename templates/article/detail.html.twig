{% extends 'base.html.twig' %}

{% block title %}Hello ArticleController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

<br>

    <h2 class="text-center">Tu es bien trop curieux! </h2>
    <h4 class="text-center"> Mais voici le détail!</h4>

<br>
   
<div class="card border-warning mb-3 text-center" style="max-width: 80rem;">

 <div class="card-body">

<div class="example-wrapper">
    
    {# <h4>{{ article.id }} </h4><br> #}
    <h4>{{ article.titre }} </h4><br>
    <img src="{{ asset('images/' ~ article.image) }}" alt="Illustration" class="w-50"><br><br>
    <h4>{{ article.description }} </h4><br>

 <a href="{{ path('article') }}"><button class="btn btn-warning">Retour</button></a>

</div>
</div>  
</div>
</div>
{% endblock %}
